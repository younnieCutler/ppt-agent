node_modules/
.DS_Store
*.pdf
dist/
output/
.ppt-agent/
.scratch/
*.pptx
*.pptm
*.png
*.jpg
*.jpeg

# Real-world regression baselines are fixtures, not build output: they must stay under version
# control so later versions can be compared against a real pre-improvement deck.
!evals/real-world/**/baseline/*.pdf
!evals/real-world/**/baseline/*.pptx

# Scratch probes
probe.tmp.*

# Organization template packs (sensitive brand/layout data)
organizations/

# Run-scoped scratch output (superseded by the hidden .ppt-agent/runs/ workspace)
artifacts/
